package org.example.model;

import java.util.logging.Logger;

public class ComplexCalculatorLogger {
    private static final Logger logerComp = Logger.getLogger(ComplexCalculatorLogger.class.getName());
}